//Reverse of a number
#include <stdio.h>

int main() {
int a,b,reverse=0;
printf("Enter the limit:");
scanf("%d",&a);
while(a>0)
{
    b=a%10;
    reverse=reverse*10+b;
    a=a/10;
}
printf("reverse=%d",reverse);

    return 0;
}
